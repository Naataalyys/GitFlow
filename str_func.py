def new_word(word = input().upper()):
  """Функция, принимающая на вход строку и возвращающая ее с заглавными буквами"""
  return word

print(new_word())

def new_text(text = input().title()):
  """Функция, которая делает заглавными первые буквы каждого слова, поступившего на вход"""

  return text

  print(new_text())
